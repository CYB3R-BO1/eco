# Integration tests run against the docker-compose data tier (Postgres, Redis,
# Neo4j). Add tests here that exercise /ready, real session use, Cypher queries
# against the GraphService, etc. Phase 1 leaves this empty by design.
